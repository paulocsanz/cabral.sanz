{
  "posts": [
    {
      "slug": "pedra-que-guarda-dados",
      "date": "2026-08-30",
      "draft": true,
      "titlePt": "PedraDB: a pedra que guarda dados",
      "titleEn": "PedraDB: the stone that keeps your data",
      "bodyPt": "<p>Nos últimos tempos venho construindo o <a href=\"https://github.com/paulocsanz/pedradb\">PedraDB</a>, um armazenamento chave-valor ordenado e embutido, com transações ACID de múltiplas chaves — Rust puro, API pequena, durável por padrão. Este post é um tour pelo que ele é, por que existe e como sei que funciona.</p><h2>O problema</h2><p>O RocksDB é um LSM rápido, mas não tem transações de verdade. Todo sistema construído em cima dele — CockroachDB, TiKV e cia — acabou reinventando consistência do zero. A ideia do PedraDB é colocar o ACID multi-chave no núcleo: atualizar a linha <em>e</em> o índice secundário é um commit só, atômico, com fsync antes do <code>Ok</code>.</p><pre><code>open → begin → get / put / delete / range → commit</code></pre><h2>O que vem na caixa</h2><ul><li>Transações multi-chave no kernel, mais transações otimistas multi-escritor (<code>begin_occ</code>).</li><li>Durável por padrão: commit dá fsync no WAL; um sync que falha <em>cerca</em> (fence) o handle em vez de seguir em silêncio.</li><li>Snapshots e MVCC com janela de 24 h, change feed durável para watch/CDC, e CAS de primeira classe — os ganchos que leases e leader locks precisam.</li><li>Um LSM de verdade embaixo: MemTable, SSTs com Bloom + lz4, compaction leveled, MANIFEST, checksums; valor-log estilo WiscKey opcional para valores grandes.</li><li>Operação embutida: backup base + incremental, WAL shipping, restore point-in-time, checkpoints e o CLI <code>pedra</code>.</li><li>Rust puro: <code>#![forbid(unsafe_code)]</code> no kernel, io_uring no Linux com fallback POSIX. O RocksDB é usado só como <em>oráculo de teste</em> — nenhum C++ é linkado.</li></ul><h2>Como sei que funciona</h2><p>Correção é o produto, então a maior parte do repositório é máquina de verificação. Clusters inteiros rodam dentro de um simulador determinístico por seed; campanhas noturnas replayam milhares de seeds com injeção de falhas (fsync mentiroso, escrita curta, WAL rasgado) e checagem de invariantes entre nós — split-brain, ressurreição e erro silencioso em zero. Há testes de oráculo contra um modelo e contra o RocksDB real, exploração de escalonamentos concorrentes estilo PCT, sanitizers (Miri, TSan, ASan) no CI e — a parte favorita — os caminhos de commit provados em Verus e Lean, rodando em modo verificado.</p><h2>Honestidade</h2><p>O que o PedraDB <em>não</em> alega: paridade de recursos com RocksDB, Pebble ou FoundationDB — esses têm anos de exposição em produção que ele não tem. A comparação honesta mora na <a href=\"https://github.com/paulocsanz/pedradb/blob/main/docs/robustness-vs-rocks-pebble-fdb.md\">doc de robustez</a>. Se você quer um cache, use Redis; SQL distribuído pronto, TiDB ou CockroachDB; B-tree estável hoje, redb. Se você quer uma biblioteca embutida, com KV ordenado durável e commits atômicos multi-chave sem C++ no hot path — aí sim, a pedra é sua.</p><h2>E depois</h2><p>O kernel é local de propósito. O <a href=\"https://github.com/paulocsanz/pedradb/blob/main/docs/montanhadb.md\">MontanhaDB</a> é a família multi-nó construída por embutimento: Raft multi-range sobre o kernel, com camadas de SQL, HTTP e streams por cima. A estrela guia é uma plataforma classe-Postgres que substitui a <em>necessidade</em> de Scylla + ClickHouse + NATS num produto só. Mas isso é tema para outro post.</p>",
      "bodyEn": "<p>For the past while I&rsquo;ve been building <a href=\"https://github.com/paulocsanz/pedradb\">PedraDB</a>, an embedded ordered key-value store with multi-key ACID transactions — pure Rust, tiny API, durable by default. This post is a tour of what it is, why it exists, and how I know it works.</p><h2>The problem</h2><p>RocksDB is a fast LSM, but it has no real transactions. Every system built on it — CockroachDB, TiKV, and friends — had to reinvent consistency from scratch. PedraDB&rsquo;s idea is to put multi-key ACID in the core: updating the row <em>and</em> its secondary index is one atomic commit, fsynced before returning <code>Ok</code>.</p><pre><code>open → begin → get / put / delete / range → commit</code></pre><h2>What&rsquo;s in the box</h2><ul><li>Kernel-level multi-key transactions, plus optimistic multi-writer transactions (<code>begin_occ</code>).</li><li>Durable by default: commit fsyncs the WAL; a failed sync <em>fences</em> the handle instead of silently continuing.</li><li>Snapshots and MVCC with a 24 h window, a durable change feed for watch/CDC, and first-class CAS — the hooks leases and leader locks need.</li><li>A real LSM underneath: MemTable, SSTs with Bloom + lz4, leveled compaction, MANIFEST, checksums; opt-in WiscKey-style value log for large values.</li><li>Ops built in: base + incremental backup, WAL shipping, point-in-time restore, checkpoints, and the <code>pedra</code> CLI.</li><li>Pure Rust: <code>#![forbid(unsafe_code)]</code> in the kernel, io_uring on Linux with POSIX fallback. RocksDB is used only as an external <em>test oracle</em> — no C++ is linked.</li></ul><h2>How I know it works</h2><p>Correctness is the product, so most of the repo is verification machinery. Whole clusters run inside a seeded deterministic simulator; nightly campaigns replay thousands of seeds with fault injection (lying fsync, short writes, torn WALs) and cross-node invariant checks — split-brain, resurrection, and silent-wrong at zero. There are oracle tests against a model and against real RocksDB, PCT-style concurrent schedule exploration, sanitizers (Miri, TSan, ASan) in CI and — my favorite part — the commit paths proven in Verus and Lean, running in verified mode.</p><h2>Honesty</h2><p>What PedraDB does <em>not</em> claim: feature parity with RocksDB, Pebble, or FoundationDB — those have years of production exposure it doesn&rsquo;t. The honest comparison lives in the <a href=\"https://github.com/paulocsanz/pedradb/blob/main/docs/robustness-vs-rocks-pebble-fdb.md\">robustness doc</a>. If you want a cache, use Redis; distributed SQL out of the box, TiDB or CockroachDB; a stable B-tree today, redb. If you want an embedded library with durable ordered KV and multi-key atomic commits, with no C++ in the hot path — then the stone is yours.</p><h2>And beyond</h2><p>The kernel is local-only on purpose. <a href=\"https://github.com/paulocsanz/pedradb/blob/main/docs/montanhadb.md\">MontanhaDB</a> is the multi-node family built by embedding it: multi-Raft ranges over the kernel, with SQL, HTTP, and stream layers on top. The north star is a Postgres-class platform that replaces the <em>need</em> for Scylla + ClickHouse + NATS in one product. But that&rsquo;s a story for another post.</p>"
    }
  ]
}
